# Звіт до роботи №2

## Тема
Основи програмування на Python

## Мета роботи
Ознайомитись з основними конструкціями мови Python, навчитися використовувати базові типи даних, умовні оператори, цикли, обробку помилок, контекст-менеджери та lambda-функції.

---

## Хід роботи

У ході виконання роботи було створено файл `robota_2.py`, у якому реалізовано приклади з основних тем мови Python.

### Виконані завдання:

- Оголошено та використано основні типи даних (`int`, `str`, `list`, `dict`)
- Продемонстровано використання вбудованих констант та функцій
- Реалізовано цикл `for`
- Реалізовано розгалуження `if / else`
- Опрацьовано конструкцію обробки помилок `try / except / finally`
- Продемонстровано роботу контекст-менеджера `with`
- Створено та використано lambda-функцію

### Приклад коду

```python
text = "Привіт, Python"
num = 5

if num > 0:
    print("Число додатне")
else:
    print("Число відʼємне")

hello = lambda name: f"Привіт, {name}!"
print(hello("Назар"))

